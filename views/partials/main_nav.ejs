 <div class="auto-style1"><p><a class="nav-link active" aria-current="page" href="/login">Login</a>&emsp;</p></div>
 
 <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <%# Navigation bar %>
        <a class="navbar-brand" href="/"><img src="/Assets/images/Initials.png" width="100" height="100"/></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">

            <% if (title == "Home") { %>
              <li class="nav-item active">
            <% } else { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link active" aria-current="page" href="/home">Home</a>
            </li>
            
             <% if (title == "About Me") { %>
              <li class="nav-item active">
            <% } else { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link active" aria-current="page" href="/about">About Me</a>
            </li>

             <% if (title == "Projects") { %>
              <li class="nav-item active">
            <% } else { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link active" aria-current="page" href="/projects">Projects</a>
            </li>

             <% if (title == "Services") { %>
              <li class="nav-item active">
            <% } else { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link active" aria-current="page" href="/services">Services</a>
            </li>

             <% if (title == "Contact Me") { %>
              <li class="nav-item active">
            <% } else { %>
            <li class="nav-item">
            <% } %>
              <a class="nav-link active" aria-current="page" href="/contact">Contact Me</a>
            </li>
      </div>
    </nav>